/**
 * The support contact address referenced from the Privacy Policy, Terms of
 * Service, and the navbar's "Support" menu item (which falls back to a
 * mailto: link when the Tawk.to chat widget isn't available — see
 * components/navbar/AuthButton.tsx). Needs a real inbox behind it (see
 * README's Bot protection/Getting Started sections) — this file only holds
 * the address, not anything that sends mail.
 */
export const SUPPORT_EMAIL = "support@quickresumebuilder.online";
